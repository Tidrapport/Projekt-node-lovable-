# ui-kit

Shared UI components used by `tidrailwork-main` and `qms-frontend` to guarantee identical look & feel.

Build:

```bash
cd ui-kit
npm install
npm run build
```

In other projects (e.g. `qms-frontend`) you can reference it via `file:../ui-kit` in `package.json` and run `npm install`.
